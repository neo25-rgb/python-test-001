# Python Shapes and Patterns Assessment

## 📋 Overview

**Estimated Time:** 2.5 hours

---

## 🎯 Learning Objectives
By completing this assessment, you will demonstrate proficiency in:
- Writing functions with parameters and return values
- Using nested loops for pattern generation
- String manipulation and concatenation
- File I/O operations
- Mathematical operations and algorithms
- Working with lists and data structures

---

## 📁 Project Structure
```
project/
│
├── main.py                 # Your solution code goes here
├── grades.txt                  # Sample data file (auto-generated by tests)
├── README.md                   # This file
├──tests/
   └──test_main.py     # Unit tests (DO NOT MODIFY)
```

---

## 🚀 Getting Started

### Prerequisites
- Python 3.7 or higher
- Basic understanding of:
  - Functions
  - Loops (for, while)
  - Conditionals (if/else)
  - String operations
  - File handling

---

## 📝 Function Requirements

### 1. `draw_square(size, filled=False, char="*")`

Draw a square of the given size.

**Parameters:**
- `size` (int): Side length of the square
- `filled` (bool): If True, fill the square; if False, make it hollow
- `char` (str): Character to use for drawing

**Example:**
```python
print(draw_square(4, filled=False))
# Output:
# ****
# *  *
# *  *
# ****
```

---

### 2. `draw_triangle(size, filled=False, char="*")`


Draw a triangle of the given height.

**Parameters:**
- `size` (int): Height of the triangle
- `filled` (bool): If True, fill the triangle; if False, make it hollow
- `char` (str): Character to use for drawing

**Example:**
```python
print(draw_triangle(4, filled=True))
# Output:
#    *
#   ***
#  *****
# *******
```
---

### 3. `draw_number_triangle(height)`

Draw a triangle of sequential numbers.

**Parameters:**
- `height` (int): Height of the triangle

**Example:**
```python
print(draw_number_triangle(4))
# Output:
#
# 1
# 2 3
# 4 5 6
```
---

### 4. `factorial(n)`
**Difficulty:** ⭐ Easy

Calculate the factorial of n (n!).

**Parameters:**
- `n` (int): Non-negative integer

**Returns:**
- `int`: n! = n × (n-1) × (n-2) × ... × 2 × 1

---

### 5. `bar_graph()`
**Difficulty:** ⭐⭐⭐ Hard

Read student grades from `grades.txt` and display a bar graph of class averages.

**Parameters:** None

**Returns:**
- `str`: Formatted bar graph

**File Format:**
- `grades.txt` contains 8 rows (one per class)
- Each row has 15 comma-separated grades
- Each star (*) represents 10%

**Example Output:**
```
Class averages:
1: ********
2: ***
3: ******
4: ***
5: ********
6: *****
7: *****
8: **
```

---

### 6. `pascals_triangle(rows)`
**Difficulty:** ⭐⭐⭐ Hard

Generate a specific row of Pascal's triangle using the formula: **p(n, k) = n! / (k! × (n-k)!)**

**Parameters:**
- `rows` (int): The row number (starting from 0)

**Returns:**
- `list`: List of integers representing that row

**Example:**
```python
pascals_triangle(5)  # Returns: [1, 5, 10, 10, 5, 1]
pascals_triangle(0)  # Returns: [1]
```

**Pascal's Triangle Visualization:**
```
Row 0:              1
Row 1:            1   1
Row 2:          1   2   1
Row 3:        1   3   3   1
Row 4:      1   4   6   4   1
Row 5:    1  5  10  10   5   1
```

**Tips:**
- Use your `factorial()` function
- For row `n`, calculate `n+1` values (columns 0 to n)
- Apply the formula for each position: `n! / (k! × (n-k)!)`
- Use integer division (`//`) to avoid floats

---

**Good luck! You've got this! 🚀**

---

*Last updated: November 2025*  
*Created by: Tech Mentor Team*
